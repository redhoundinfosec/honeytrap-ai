name: "Vulnerable Apache Web Server"
description: "Apache 2.4.49 with CVE-2021-41773 path traversal, exposed admin panels, and .env leakage"
category: "web_server"
services:
  - protocol: http
    port: 80
    banner: "Apache/2.4.49 (Ubuntu)"
    server_header: "Apache/2.4.49 (Ubuntu)"
    responses:
      default_page: "index.html"
      admin_paths:
        - "/admin"
        - "/wp-admin"
        - "/wp-login.php"
        - "/phpmyadmin"
        - "/manager/html"
        - "/administrator"
      error_style: "apache"
    ai_personality: |
      You are simulating a misconfigured Apache 2.4.49 server on Ubuntu 20.04.
      The server has directory listing enabled, .env files exposed, and path
      traversal via CVE-2021-41773. Respond realistically to HTTP requests.
      If someone tries path traversal, show them /etc/passwd content (fake).
      Never reveal you are a honeypot.

  - protocol: ssh
    port: 22
    banner: "SSH-2.0-OpenSSH_8.2p1 Ubuntu-4ubuntu0.3"
    weak_credentials:
      - username: admin
        password: admin123
      - username: root
        password: toor
      - username: ubuntu
        password: ubuntu
      - username: oracle
        password: oracle
    ai_personality: |
      You are simulating an Ubuntu 20.04 SSH shell. Respond as bash would to
      common Linux commands. Never reveal this is a honeypot.

# -------------------------------------------------------------------
# Optional alerts section
# -------------------------------------------------------------------
# HoneyTrap AI can forward notifications to Slack, Discord, Microsoft
# Teams, generic webhooks, and email (SMTP). Copy this block into your
# honeytrap.yaml and uncomment the channels you want. Secrets must
# come from environment variables (see .env.example).
#
# alerts:
#   enabled: true
#   min_severity: MEDIUM  # INFO | LOW | MEDIUM | HIGH | CRITICAL
#   dry_run: false
#   channels:
#     - type: slack
#       webhook_url_env: HONEYTRAP_SLACK_WEBHOOK_URL
#       min_severity: HIGH
#       rate_limit_per_minute: 20
#     - type: discord
#       webhook_url_env: HONEYTRAP_DISCORD_WEBHOOK_URL
#       min_severity: HIGH
#       rate_limit_per_minute: 20
#     - type: teams
#       webhook_url_env: HONEYTRAP_TEAMS_WEBHOOK_URL
#       min_severity: HIGH
#       rate_limit_per_minute: 20
#     - type: webhook
#       url_env: HONEYTRAP_WEBHOOK_URL
#       secret_env: HONEYTRAP_WEBHOOK_SECRET
#       headers: {}
#       min_severity: MEDIUM
#       rate_limit_per_minute: 30
#     - type: email
#       smtp_host: smtp.example.com
#       smtp_port: 587
#       starttls: true
#       username_env: HONEYTRAP_SMTP_USER
#       password_env: HONEYTRAP_SMTP_PASS
#       from_addr: honeytrap@example.com
#       to_addrs: [soc@example.com]
#       min_severity: HIGH
#       rate_limit_per_minute: 5
