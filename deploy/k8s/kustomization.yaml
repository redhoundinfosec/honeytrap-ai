apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: honeytrap

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  # Uncomment once Prometheus Operator CRDs are installed:
  # - servicemonitor.yaml

commonLabels:
  app.kubernetes.io/name: honeytrap-ai
  app.kubernetes.io/managed-by: kustomize
