"""Performance benchmarks for HoneyTrap hot-path components.

Benchmarks live behind the ``benchmark`` pytest marker so they are
excluded from default test runs. Invoke explicitly with::

    pytest tests/bench/ --benchmark-only
"""
